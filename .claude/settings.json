{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "pgrep -f 'vite' >/dev/null || (cd /home/barry/lynk && nohup npm run dev > /tmp/lynk-dev.log 2>&1 &) ; echo '{\"systemMessage\":\"lynk dev: '$(pgrep -f vite >/dev/null && echo running || echo started)' (logs: /tmp/lynk-dev.log)\"}'",
            "async": true
          }
        ]
      }
    ]
  }
}
